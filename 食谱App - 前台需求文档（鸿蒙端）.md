# 食谱 App - 前台需求文档（鸿蒙端）

更新时间：2026-08-21

相关文档：`harmony-app-architecture.md`（鸿蒙端技术框架）、`harmony-app-implementation-guide.md`（实施文档，含设计规范与接口契约）、`recipe-app-requirements.md`（后台需求）、`eat_schema.sql`（数据库设计）

------

## 一、信息架构

底部四个 Tab：

```
首页        内容发现：搜索、标签入口、今日推荐、热门榜、最新发布
分类        按标签浏览食谱，支持排序与二次筛选
AI          对话式入口，承载食材推荐 / 条件筛选 / 兜底生成
我的        个人内容与设置
```

**为什么 AI 单独占一个 Tab**：食谱类 App 同质化严重，本产品真正的差异点是"我冰箱里有这几样东西，能做什么菜"。这个能力如果藏在首页卡片或搜索框里，用户发现不了，等于白做。放 Tab 上是把差异化摆到最显眼的位置。

------

## 二、全局规则

### 视觉风格

**小清新**——低饱和自然色调、大留白、柔和圆角、极轻阴影、线性图标。主色取清新绿，点缀色取暖橙。完整设计 token（色值、圆角、间距、字号、阴影）见 `harmony-app-implementation-guide.md` 第二节。

第一期**只做浅色模式**，但色值全部定义在资源文件中，`resources/dark/` 目录结构预留，后续加暗色模式不需要改业务代码。

### 游客与登录

沿用后台需求文档定的权限规则，**不做启动强制登录**：

| 行为                       | 是否需要登录 |
| -------------------------- | ------------ |
| 浏览首页、分类、详情、搜索 | 否           |
| 收藏、点赞                 | 是           |
| 使用 AI 功能               | 是           |
| 我的 Tab                   | 是           |

**登录拉起方式**：App 启动直接进首页，不拦截。用户触发受限动作时，**半屏弹出登录**，登录成功后自动继续原动作（例如点收藏 → 登录 → 收藏成功）。未登录进入"我的" Tab 时，展示登录引导页而非空白。

> 强制登录会大幅降低留存，且应用市场审核对"未提供必要功能前强制登录"本身也是敏感项。

### 其他全局约定

- 所有列表页统一四态：加载中（骨架屏）/ 有数据 / 空状态 / 加载失败（可重试）
- 列表分页统一用**下拉加载更多**，不做页码
- 图片统一走缩略图 URL，详情页封面才加载中图
- **第一期不做消息推送/通知**

------

## 三、登录注册

### 登录方式

| 方式             | 第一期 | 说明                                                       |
| ---------------- | ------ | ---------------------------------------------------------- |
| 华为账号一键登录 | ✅ 主推 | Account Kit，鸿蒙标配体验，应用市场加分项                  |
| 手机验证码       | ✅      | 未注册手机号自动创建账号                                   |
| 账号密码         | ✅      |                                                            |
| 邮箱             | ✅      |                                                            |
| 微信登录         | ❌ 不做 | 鸿蒙 NEXT 上的 SDK 支持需单独确认，留到做安卓/小程序时再接 |

### 页面要求

- 两种形态：**半屏弹出**（触发受限动作时）和**完整页**（从"我的"进入）
- **必须有用户协议与隐私政策勾选项**，未勾选不可登录——应用市场硬性要求
- 昵称头像：华为账号登录可授权获取；其他方式注册后给默认昵称（如"用户1234"）和默认头像，用户可在"我的"里修改

------

## 四、首页

自上而下五段式，**完全靠现有数据撑起，后台无需新增运营配置功能**：

| 模块            | 内容                                                    | 数据来源                |
| --------------- | ------------------------------------------------------- | ----------------------- |
| 1. 搜索框       | 点击进搜索页                                            | —                       |
| 2. 标签快捷入口 | 宫格，取关联食谱数最多的 8 个标签 + "更多"跳分类页      | `recipe_tag`            |
| 3. AI 引导卡    | 一张显眼卡片："冰箱里有什么？让 AI 帮你配菜"，跳 AI Tab | —                       |
| 4. 今日推荐     | 6 道菜，横滑卡片                                        | 见下方说明              |
| 5. 热门榜单     | 收藏 TOP10，横滑卡片                                    | `recipe.favorite_count` |
| 6. 最新发布     | 竖向列表流，下拉加载更多                                | `recipe.publish_time`   |

支持下拉刷新。

### 关于"今日推荐"

**第一期不做个性化推荐。** 日活约 50，一天产生的浏览和收藏行为可能只有几百条，这个数据量喂不出有意义的推荐结果，做出来和随机排序区分不开。

替代方案：后端用**按日期做种子的伪随机**从已发布食谱中取 6 道——每天换一批、当天所有用户看到相同内容、实现只需几行代码。等有真实行为数据积累后再升级为推荐算法。

### 不做 Banner 轮播位

后台目前没有"运营位管理"菜单，要做就得配套一整套（图片、跳转配置、排序、上下线、生效时间），投入产出比过低。等内容量和用户量起来后再评估。

------

## 五、分类页

### 布局

- 顶部：标签横滑选择栏（全部 / 早餐 / 主食 / 粥品 …）
- 其下：排序与筛选栏（收起式）
- 主体：**双列瀑布流卡片**。食谱是图片主导内容，双列的信息密度明显优于单列

### 卡片字段

封面图、菜名、难度（**星级图标，不用文字**）、预估耗时、收藏数、点赞数。

> 加入耗时的理由：对下班做饭的用户来说，"20分钟"是比难度更强的决策依据。

### 排序与筛选

- **排序**：最新发布 / 最多收藏 / 最多点赞 —— 对应 `publish_time` / `favorite_count` / `like_count`，索引已存在
- **筛选**：难度（星级多选）、预估耗时（区间）

### 交互

- 下拉加载更多
- 点击卡片进详情页
- 空状态：该标签下暂无食谱

------

## 六、详情页

自上而下：

1. **封面大图**
2. **标题区**：菜名、标签、难度星级、预估耗时、**每份热量**（`calorie / base_servings`）
3. **简介**（`recipe.summary`）
4. **份数切换器** —— 见下方说明
5. **食材列表**：食材名、用量、单位、备注；可选食材标注"可选"
6. **制作步骤**：序号、描述、配图
7. **附加提示**（`recipe_tip`，含 AI 生成的技巧提示）
8. **底部固定操作栏**：收藏、点赞、分享

未登录点收藏/点赞 → 半屏拉起登录，登录后自动完成动作。

### 份数切换器

后台数据结构已经支持按份换算（`recipe.base_servings` + `recipe_ingredient.is_convertible`），前台必须把这个能力用起来：

- 默认展示基准份数
- 用户可加减份数，**食材用量实时联动换算**
- `is_convertible = 0` 的食材（如"盐适量"）**不随份数变化**
- 每份热量不随份数变化（总热量按比例变，每份不变）

### 分享

**第一期使用系统分享**（Share Kit 拉起系统分享面板），分享内容为食谱名称 + 简介 + 落地页链接。不做生成分享图片、不做小程序卡片——这两项等有对应端和落地页时再评估。

### AI 技巧提示的落地方式

> **不新增"AI 总结"独立模块，AI 生成的技巧提示合并进已有的 `recipe_tip`。**

理由：`recipe_tip` 表现有数据本身就是"关键技巧与坑点"（如"煎鱼时全程保持中小火"），性质完全重合。做法改为——**运营在后台编辑食谱时点击「AI 生成技巧提示」按钮**，模型读取该食谱的食材和步骤，产出 3-5 条提示填入 tips 子表单，编辑修改确认后保存。前台详情页展示的仍是原有的提示模块。

这样做的收益：

- 数据库不加字段，前台不加模块
- 避免详情页出现"附加提示"和"AI 总结"两块内容雷同的区域，让用户困惑
- AI 的价值落在**降低编辑录入成本**上，而非增加用户的阅读负担
- 提示内容经过人工过滤，质量可控，且是**一次性生成、永久复用**，不产生重复调用成本

------

## 七、AI 页

### 形态

**对话式界面**，两个场景在同一入口自然分流，不让用户先选"我要用哪个功能"：

- 用户输入"我有鸡蛋、番茄、面条" → 意图判定为**食材推荐**
- 用户输入"想吃低卡的健身餐" → 意图判定为**条件筛选**

首次进入展示示例气泡引导（如"我有鸡蛋和番茄"、"半小时能做好的家常菜"、"低卡健身餐"）。

### 结果呈现

| 场景     | 返回内容                                                     |
| -------- | ------------------------------------------------------------ |
| 食材推荐 | 按食材重合度从高到低的食谱卡片列表，每张卡片标注重合度和"还缺 XX 食材" |
| 条件筛选 | 按结构化条件匹配的食谱卡片列表                               |
| 兜底生成 | 完整的生成食谱内容（菜名、食材、步骤、提示、热量），可收藏   |

### 兜底生成规则（沿用后台需求文档）

- 触发条件：食材重合度低于 60%，或条件筛选无匹配结果
- 生成食材限定在食材库范围内，热量按"食材营养成分 × 用量"自动计算，数据可信
- 生成内容标注"AI 生成"身份，与正式食谱区分；不进入 `recipe` 表，不计入榜单
- 用户可收藏，存入 `ai_generated_recipe_favorite`，在"我的 AI 食谱"中查看

### 技术要求

- 输出采用**流式返回**，边生成边展示，避免长时间空等
- 支持中途取消
- AI Tab 需登录，未登录点击 Tab 时半屏拉起登录

------

## 八、我的

### 未登录

展示登录引导页（一句价值说明 + 登录按钮），不展示空的功能列表。

### 已登录

| 分组         | 内容                                               |
| ------------ | -------------------------------------------------- |
| **头部**     | 头像、昵称、收藏数 / 点赞数；点击进资料编辑        |
| **我的内容** | 我的收藏、我的点赞、浏览历史、我的 AI 食谱         |
| **设置**     | 账号与绑定、清除缓存、关于我们、用户协议、隐私政策 |
| **底部**     | 退出登录                                           |

### 两个要点

**浏览历史只存本地**（鸿蒙 `preferences`），不建后端表。第一期只有一个端，本地存储足够；等后续做安卓/小程序需要跨端同步时再加表，那时候补也不晚。

⚠️ **「账号与绑定」中必须包含「注销账号」入口**。这是华为应用市场上架的**强制要求**，不是可选项，遗漏会被审核打回。注销需二次确认，并明确告知数据删除范围。

------

## 九、搜索页

- 顶部搜索框，自动聚焦
- 搜索历史（**本地存储**，可清空）
- 无历史时展示热门食谱（复用首页热门榜数据）
- 搜索结果卡片样式与分类页一致
- 空结果时引导跳转 AI Tab："没找到？让 AI 帮你想想"

------

## 十、后端与后台变更清单

本文档引出的改动，需同步到 `recipe-app-requirements.md` 和 `eat_schema.sql`：

| #    | 变更                                                         | 位置              | 说明                                                         |
| ---- | ------------------------------------------------------------ | ----------------- | ------------------------------------------------------------ |
| 1    | **`recipe` 表新增 `publish_time`**                           | schema            | 首页/分类页的"最新"排序需要。用 `create_time` 不准——草稿创建时间不等于发布时间。发布操作时写入 |
| 2    | **`ai_call_log.user_id` 改为可空，新增 `sys_user_id` 与 `caller_type`** | schema            | ⚠️ 现为 `NOT NULL` 且指向 `app_user`。但"AI 生成技巧提示"是**后台运营触发**的，没有对应的前台用户。需区分调用方（`APP_USER` / `SYS_USER`），否则该场景无法记入调用日志、成本监控出现盲区 |
| 3    | `ai_prompt_template.scene` 新增 `RECIPE_TIPS`                | schema + 后台 3.2 | 后台"Prompt模板管理"的应用场景下拉增加"食谱技巧提示生成"     |
| 4    | `app_user_oauth.platform` 枚举增加 `HUAWEI`                  | schema 注释       | 华为账号登录                                                 |
| 5    | 食谱编辑抽屉增加「AI 生成技巧提示」按钮                      | 后台 1.1          | 生成结果填入 tips 子表单，供编辑修改后保存                   |
| 6    | 登录方式调整：微信登录第一期不做                             | 后台 4.1          | 改为华为账号一键登录，见第三节                               |
| 7    | AI 模型配置无需支持多模态                                    | 后台 3.1          | 拍照识别第一期不实现，选型只需考虑纯文本模型                 |
| 8    | 食谱列表接口需支持排序参数                                   | 接口              | 最新 / 最多收藏 / 最多点赞                                   |
| 9    | 图片需提供多尺寸 URL                                         | 接口              | 缩略图 / 中图 / 原图，上传时生成多规格。列表页禁止加载原图   |

------

## 十一、已确认的产品决策

| 决策项         | 结论                                             |
| -------------- | ------------------------------------------------ |
| UI 视觉风格    | **小清新**，完整设计 token 见实施文档第二节      |
| 暗色模式       | 第一期不做，资源结构预留                         |
| 分享形式       | **系统分享**（Share Kit），不做分享图/小程序卡片 |
| 消息通知与推送 | **第一期不做**                                   |
| 拍照识别食材   | **第一期不做**，食材输入只支持文字               |
| 个性化推荐     | 第一期不做，"今日推荐"用按日期种子的伪随机       |
| Banner 轮播位  | 第一期不做                                       |
| 浏览历史       | 只存本地，不建后端表                             |

------

## 十二、待定项

1. **UI 设计稿** —— 风格已定为小清新、设计 token 已给出，但具体页面视觉稿待 UI 产出。
2. **安卓 / 微信小程序的跨端方案选型** —— 鸿蒙完成后再评估。