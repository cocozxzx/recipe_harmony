# 食谱 App - 鸿蒙端技术框架

更新时间：2026-08-21

相关文档：`recipe-app-tech-stack.md`（后台技术栈）、`recipe-app-requirements.md`（需求文档）、`eat_schema.sql`（数据库设计）

------

## 一、决策记录

| 决策项          | 结论                                          | 说明                                                         |
| --------------- | --------------------------------------------- | ------------------------------------------------------------ |
| 面向用户端形态  | **只做 App，不做 Web 前台**                   | 需求文档里"前台是否需要 SEO"这个待定项作废                   |
| 端覆盖顺序      | **鸿蒙原生优先**，安卓 + 微信小程序后续走跨端 | 鸿蒙做完再单独评估跨端方案                                   |
| 鸿蒙开发方式    | **原生 ArkTS + ArkUI**                        | 不走 uni-app x / Taro                                        |
| 目标系统        | **HarmonyOS NEXT（纯血鸿蒙）**                | 不兼容安卓 APK，不维护双包                                   |
| API 基线        | **API 23（HarmonyOS 6.1.0）**                 | 覆盖约 96.9% 存量设备                                        |
| 设备形态        | **第一期只声明 phone**                        | 代码用响应式写法，但不出大屏设计稿                           |
| AI 拍照识别食材 | **第一期不实现**                              | 食材输入只支持文字，不申请相机/相册权限，后台模型配置无需支持多模态 |

### 为什么鸿蒙不走跨端

- **uni-app x** 虽然 4.61+ 支持 HarmonyOS NEXT，但官方明确说明"处于早期阶段，不应以 Android/iOS 的标准验收"，且**尚未发布小程序 SDK**——"一套代码出三端"的前提当下并不成立。
- **Taro 4.1+** 支持鸿蒙 ArkTS，小程序端成熟度高，是三端方案里最对路的；但鸿蒙适配属新增能力，连 DevEco 预览器都无法使用，遇到鸿蒙侧问题基本只能等上游。
- 第一期日活约 50，跨端复用省下的工时远小于踩坑成本。
- 后续做安卓/小程序时，**真正能复用的是后端接口和产品设计，不是 UI 代码**。只要接口按通用 REST 设计、不绑死端，这部分复用现在就已经拿到了。

------

## 二、工程结构

第一期不必真拆成 HAR/HSP 模块，但目录按下述边界分好，以后要拆成本接近 0。

```
eat-harmony/
├── entry/                      应用壳
│   └── src/main/ets/
│       ├── entryability/       EntryAbility、生命周期
│       ├── pages/              Index（Navigation 根容器 + 底部 Tab）
│       └── router/             路由表注册、NavPathStack 全局实例
├── features/                   业务模块（按领域划分）
│   ├── recipe/                 食谱列表、详情、搜索、标签筛选
│   ├── ai/                     食材推荐、条件筛选、兜底生成结果
│   └── user/                   登录注册、收藏/点赞、个人中心
├── commons/                    通用能力
│   ├── network/                HttpClient、拦截器、错误码、响应基类
│   ├── storage/                preferences 封装、Token 管理
│   ├── uikit/                  通用组件：四态容器、骨架屏、星级、图片、下拉刷新
│   └── utils/                  日志、日期、格式化
└── products/                   预留：一多分发工程（第一期不用）
```

**模块依赖方向必须单向**：`entry` → `features` → `commons`。`features` 之间**不允许互相直接引用**，需要跨模块跳转时通过路由表用字符串路由名，需要共享数据时下沉到 `commons`。这条纪律是以后能拆 HAR 的唯一前提。

------

## 三、分层架构

鸿蒙没有官方 MVVM 框架，但状态管理 V2 天然就是这个形状：

```
View（ArkUI 组件，@ComponentV2）
   ↕  @Local / @Param / @Event
ViewModel（@ObservedV2 类，@Trace 标记可观察属性）
   ↕
Repository（编排数据来源：网络 / 本地缓存）
   ↕
DataSource（RCP 网络请求 / preferences / relationalStore）
```

### 各层职责

- **View**：只负责渲染和事件转发，**不写业务逻辑、不直接发请求**。
- **ViewModel**：持有页面状态（`PageState`、列表数据、表单值）和业务动作（加载、刷新、提交）。一个页面一个 ViewModel，用 `@ObservedV2` 类实现，需要被观察的字段加 `@Trace`。
- **Repository**：决定数据从网络拿还是从缓存拿，做 DTO → 领域模型的转换。**JSON 反序列化的显式转换代码集中放在这一层**，不要散落到 ViewModel。
- **DataSource**：纯粹的 IO，不含业务判断。

------

## 四、状态管理规范

统一使用**状态管理 V2**：

| 场景                           | 用法                                     |
| ------------------------------ | ---------------------------------------- |
| 组件内部状态                   | `@Local`                                 |
| 父传子                         | `@Param`（子只读）+ `@Event`（子回调父） |
| 状态变化监听                   | `@Monitor`                               |
| 跨层级传递（避免逐层透传）     | `@Provider` / `@Consumer`                |
| 可观察的数据类                 | `@ObservedV2` 类 + `@Trace` 字段         |
| 全局共享状态（登录用户等）     | `AppStorageV2`                           |
| 需要落盘的全局状态（Token 等） | `PersistenceV2`                          |

### 硬性纪律

> **V1 和 V2 装饰器绝对不能在同一个组件树里混用。**

这是鸿蒙新手最高频的坑：混用后状态更新会静默失效，且不报错，排查成本极高。项目里禁止出现 `@Component` / `@State` / `@Prop` / `@Link` / `@Observed` / `@ObjectLink` 等 V1 装饰器，代码评审时作为红线检查项。

------

## 五、路由方案

- 使用 **`Navigation` + `NavPathStack`**，禁用旧的 `router` API。
- 全局单一 `NavPathStack` 实例放在 `entry`，通过 `@Provider` 下发。
- 每个页面是一个 `NavDestination`，在路由表里以字符串名注册，跨模块跳转只依赖字符串，不产生编译期依赖。
- `Navigation` 的 `mode` 设为 `NavigationMode.Auto`：窗口宽度 ≥ 600vp 时自动从单栏堆栈变为"左列表 + 右详情"分栏。**现在写好，以后上平板/折叠屏不用改架构。**
- 页面间传参统一用类型化的参数对象，不传裸 Map。

------

## 六、网络层

### 选型

使用 **RCP（Remote Communication Kit）**，而非旧的 `@ohos.net.http`。RCP 原生支持拦截器和会话复用，封装更干净。

### 需要封装的四件事

1. **统一配置**：baseURL、超时、公共请求头、UA。
2. **Token 注入 + 401 自动刷新**。刷新逻辑**必须做单飞（single-flight）**——并发请求同时收到 401 时只允许一个刷新请求发出，其余挂起等待结果，否则会出现多次刷新互相顶掉的问题。
3. **业务错误码统一处理**：约定后端响应基类 `{ code, message, data }`，在拦截器里判断 code，非成功码统一抛业务异常，页面层只处理需要特殊提示的码。
4. **网络异常兜底**：断网、超时、5xx 统一转成用户可读文案，不把原始异常抛到 UI。

### 接口约定（与后端对齐）

- 通用 REST 风格，**不为鸿蒙端做定制接口**——后续安卓/小程序要复用。
- 列表接口统一分页参数与响应结构。
- 图片字段返回**多尺寸 URL**（缩略图 / 中图 / 原图），列表页禁止加载原图。本地磁盘存储方案下由后端在上传时生成多规格。

------

## 七、AI 流式返回（最高风险项）

需求文档中的 AI 场景（食材推荐、条件筛选、兜底生成）需要流式输出体验。第一期食材输入**只支持文字**，不做拍照识别。

- 鸿蒙**没有现成的 `EventSource`/SSE 封装**，需要用 `http` 的 `requestInStream` + `on('dataReceive')` 手动接收字节流并拼接增量数据。
- 需要自己处理：分片边界（一个 chunk 可能不是完整的一行）、`data:` 前缀解析、结束标记识别、中途取消、异常重连。

> **排期要求：第一周完成技术验证（打通一条端到端的流式链路），不要等到开发中期。** 这是整个 App 里唯一有可能推翻交互设计的技术点。

------

## 八、本地存储

| 数据                | 方案                                  |
| ------------------- | ------------------------------------- |
| Token、用户基础信息 | `preferences`（配合 `PersistenceV2`） |
| 搜索历史、轻量偏好  | `preferences`                         |
| 食谱离线缓存        | `relationalStore`——**第一期不做**     |

第一期日活 50，离线能力不是刚需，先不引入关系型数据库，避免过早增加复杂度。

------

## 九、图片与列表性能

食谱类 App 图片密度高，这块直接决定体感：

- 图片加载与缓存使用 **ImageKnife**（`@ohos/imageknife`），提供内存 + 磁盘二级缓存。
- 长列表**必须**用 `LazyForEach` + 自定义 `IDataSource`，禁止 `ForEach` 渲染大列表。
- 合理设置 `cachedCount`（建议 1~3 屏），过大反而增加内存压力。
- 列表项固定高度或提供占位尺寸，避免图片加载后布局抖动。
- 封面图走缩略图 URL（见第六节接口约定）。

------

## 十、登录与鉴权

需求文档原定"手机验证码 + 账号密码 + 邮箱 + 第三方（微信等）"。**鸿蒙端需要调整**：

- **主推：华为账号一键登录（Account Kit）**。鸿蒙上的标配体验，也是华为应用市场上架的加分项。
- 保留：手机验证码、账号密码、邮箱。
- **微信登录第一期不做**——微信开放平台在鸿蒙 NEXT 上的 SDK 支持情况需单独确认，且鸿蒙用户用华为账号更自然。留到做安卓/小程序时再接。

> 数据结构侧：后端 User 主表 + 第三方绑定表的设计不变，只是第一期先落 `huawei` 一种平台类型。

鉴权方案沿用 JWT（与后台管理系统一致），Token 存 `preferences`，刷新逻辑见第六节。

------

## 十一、多设备适配策略

`module.json5` 的 `deviceTypes` 只解决"**能不能装**"，不解决"**装上去好不好看**"。

**系统能自动处理的**：

- `Navigation` 的 `Auto` 模式自动分栏
- `GridRow` / `GridCol` 栅格按断点（xs / sm / md / lg）自动重排
- 媒体查询

**必须人工决策的**：大屏上列表几列、卡片尺寸、详情页显示哪些额外字段、图片比例、字号层级——这些需要 UI 出对应设计稿。

### 第一期策略

1. `deviceTypes` **只声明 `phone`**。一旦声明 `tablet`，应用市场上架要过平板适配审核，做不到位会被打回，凭空多一轮返工。
2. 代码**坚持响应式写法**（栅格 + `Navigation Auto`），成本几乎为 0。
3. UI **只出手机设计稿**。
4. 以后要上平板：加一行 `deviceTypes` 声明 + 补大屏设计稿，架构不动。

------

## 十二、依赖与工程配置

### 三方依赖（尽量少引）

鸿蒙 ohpm 生态仍较薄，能自己封的不引三方。

| 用途         | 方案                   |
| ------------ | ---------------------- |
| 图片加载缓存 | `@ohos/imageknife`     |
| 日志         | 自封 `hilog`，不引三方 |
| 状态管理     | 系统 V2 能力，不引三方 |
| 网络         | 系统 RCP，不引三方     |

### 多环境配置

在 `build-profile.json5` 的 `buildOption.arkOptions.buildProfileFields` 中注入 dev / prod 的 baseURL 等变量，编译期生成 `BuildProfile` 类读取。**禁止在业务代码里手改地址后提交。**

### 权限清单

第一期**不申请相机和相册权限**（拍照识别不做）。涉及的系统权限仅网络访问。后续如需选择本地图片，优先使用免权限的 Picker 方式。

------

## 十三、ArkTS 语言限制（学习成本主要来源）

> **ArkTS 不是 TypeScript 的超集。** 它为了 AOT 编译和运行时性能，砍掉了大量动态特性。

主要限制：

- `any` 的大部分用法被禁止
- 不支持 structural typing（结构化类型兼容）
- 不支持动态属性访问（`obj[key]`）
- 对象字面量必须有明确的类型声明，不能靠推导
- 运行时类型检查能力受限

**最直接的痛点**：`JSON.parse` 的结果不能像 TS 里那样直接当对象使用，**必须写显式的类定义和转换代码**。这也是为什么第三节要求把反序列化集中在 Repository 层——否则这类样板代码会污染整个项目。

**排期提示**：Vue/TS 背景的前端上手 ArkTS 预计需要 1~2 周，学习成本的大头在这套语言限制上，而不在声明式 UI 的写法上。这部分要在排期里单独列出，不要摊进开发工时。

------

## 十四、开发前置与风险清单

| 事项                     | 类型         | 说明                                                         |
| ------------------------ | ------------ | ------------------------------------------------------------ |
| AI 流式返回技术验证      | **技术风险** | 第一周必须打通，见第七节                                     |
| 软著、备案、隐私声明     | **上架前置** | 华为应用市场必需，**前置周期比开发还长，建议立即并行启动**   |
| 应用市场账号与开发者认证 | 上架前置     | 同上                                                         |
| 前端 ArkTS 学习期        | 排期         | 1~2 周，单独列出                                             |
| 原 90 天排期需重估       | 排期         | 原排期按"后台 + Web 前台"制定，前台换成鸿蒙原生 App 后需重新评估 |
| 后端多尺寸图片生成       | 依赖后端     | 见第六节接口约定                                             |

------

## 十五、待定项

1. **安卓 / 微信小程序的跨端方案选型**——鸿蒙完成后再评估。
2. **App 页面结构与信息架构**——本文只覆盖代码框架，页面清单待需求侧展开。